<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/speedPopupCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="@android:color/black"
    app:cardCornerRadius="24dp"
    app:cardElevation="4dp"
    app:contentPaddingLeft="12dp"
    app:contentPaddingTop="8dp"
    app:contentPaddingRight="12dp"
    app:contentPaddingBottom="12dp"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/speedPopupTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:includeFontPadding="false"
                android:text="@string/player_speed_title"
                android:contentDescription="@string/playback_speed"
                android:textColor="@android:color/white"
                android:textSize="18sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/currentSpeedText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:includeFontPadding="false"
                android:textColor="@android:color/white"
                android:textSize="17sp"
                android:textStyle="bold"
                tools:text="1.25x" />

            <ImageButton
                android:id="@+id/speedPopupClose"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/close"
                android:src="@drawable/baseline_close_black_36"
                app:tint="@android:color/white" />

        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/btnDecreaseSpeed"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/bg_player_speed_round_control"
                android:contentDescription="@string/speed_decrease"
                android:src="@drawable/baseline_remove_black_24"
                app:tint="@android:color/white" />

            <com.google.android.material.slider.Slider
                android:id="@+id/speedSlider"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:layout_marginEnd="12dp"
                android:layout_weight="1"
                android:valueFrom="0.25"
                android:valueTo="8.0"
                android:stepSize="0.05"
                app:haloColor="@android:color/transparent"
                app:labelBehavior="gone"
                app:tickVisible="false"
                app:thumbColor="@android:color/white"
                app:trackColorActive="@android:color/white"
                app:trackColorInactive="#2AFFFFFF"
                app:trackHeight="8dp" />

            <ImageButton
                android:id="@+id/btnIncreaseSpeed"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/bg_player_speed_round_control"
                android:contentDescription="@string/speed_increase"
                android:src="@drawable/baseline_add_black_24"
                app:tint="@android:color/white" />

        </LinearLayout>

        <LinearLayout
            android:id="@+id/speedPresetRows"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center_horizontal"
            android:orientation="vertical" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
